#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {


	vector<int>A = {1, 4, 5};
	vector<int>B = {2, 3};


	int n = A.size();
	int m = B.size();

	int len = n + m;

	// this is my comment for programme a.

	int l = 0;
	int r = len / 2 + 1;

	if (n > m) {
		int l = 0;

		while (l <= r) {
			int m = (l + (r - l)) / 2;

			int cut1 = m;
			int cut2 = ;

		}
	} else {

	}


	return 0;
}